# TODO: Phase 3 - Demystifying Docker
# Goal: Create a Dockerfile to containerize our Node.js Backend API.

# 1. Start from a lightweight Node.js base image (e.g., node:18-alpine)
# TODO: FROM ...

# 2. Set the working directory inside the container to /app
# TODO: WORKDIR ...

# 3. Copy package.json and package-lock.json to the working directory
# TODO: COPY ...

# 4. Install production dependencies only
# TODO: RUN npm ...

# 5. Copy the rest of the backend source code
# TODO: COPY ...

# 6. Expose the port your server runs on (defined in .env as 3000)
# TODO: EXPOSE ...

# 7. Define the command to start the server (npm start or node src/server.js)
# TODO: CMD [...]
